<?php

// Calculate sun of two integer numbers
function sum($a, $b) {
    return $a + $b;
}

// rewritten by me:

/**
 * Calculate sun of two integer numbers
 * @param int $a
 * @param int $b
 * @return int
 */
function sum2(int $a, int $b): int {
    return $a + $b;
}
